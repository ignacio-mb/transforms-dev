-- Transform: [name]
-- Description: [what this transform does]
--
-- Target: [schema].[table_name]
-- Source tables: [list source tables]
--
-- To test: run this query in Metabase's SQL editor before saving as a transform.

SELECT
  *
FROM source_table
WHERE 1 = 1
